import { PaginationIndexPipe } from './pagination-index.pipe';

describe('PaginationIndexPipe', () => {
  it('create an instance', () => {
    const pipe = new PaginationIndexPipe();
    expect(pipe).toBeTruthy();
  });
});
