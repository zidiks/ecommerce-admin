import { OrderHistoryPipe } from './order-history.pipe';

describe('OrderHistoryPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderHistoryPipe();
    expect(pipe).toBeTruthy();
  });
});
